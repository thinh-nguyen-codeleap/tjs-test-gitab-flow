# gitversion.yml for GitVersion 6
# Configured to support GitLab Flow with Semantic Versioning for the following branches:
# main, staging, production, bugfix, hotfix
next-version: 1.0.0
mode: ContinuousDeployment
branches:
  main:
    regex: ^main$|^master$
    label: beta
    increment: Inherit
    prevent-increment:
      of-merged-branch: false
    track-merge-target: true
    source-branches:
      - feature
      - bugfix
      - hotfix
      - staging
    is-source-branch-for:
      - staging

  staging:
    regex: ^staging$
    label: rc
    increment: Patch
    prevent-increment:
      of-merged-branch: false
    track-merge-target: true
    source-branches:
      - main
      - bugfix
      - hotfix
    is-source-branch-for:
      - main
      - production

  production:
    regex: ^production$
    label: ""
    increment: Patch
    prevent-increment:
      of-merged-branch: false
    track-merge-target: true
    source-branches:
      - staging
      - hotfix
    is-source-branch-for: []

  feature:
    regex: ^features?[/-](?<BranchName>.+)
    label: ""
    increment: Minor
    prevent-increment:
      of-merged-branch: false
    track-merge-target: true
    source-branches: []
    is-source-branch-for:
      - main

  bugfix:
    regex: ^bugfix(es)?[/-](?<BranchName>.+)
    label: ""
    increment: Patch
    prevent-increment:
      of-merged-branch: false
    track-merge-target: true
    source-branches: []
    is-source-branch-for:
      - main
      - staging

  hotfix:
    regex: ^hotfix(es)?[/-](?<BranchName>.+)
    label: ""
    increment: Patch
    prevent-increment:
      of-merged-branch: false
    track-merge-target: true
    source-branches:
      - production
    is-source-branch-for:
      - production
      - staging

ignore:
  sha: []
